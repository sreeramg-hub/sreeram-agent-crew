.env
__pycache__/
.DS_Store
.venv/
*.pyc
cookies.txt

# state/ is intentionally tracked — committed back each run
# digest.html (local preview) and metals.html (crew output) are ephemeral
digest.html
metals.html
